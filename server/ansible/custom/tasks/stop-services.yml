# Services Stop

- name: "SERVER - Start application services"
  shell:
    cmd: "docker-compose --file {{ docker_root }}/docker-compose.yml --project-name dropship --project-directory {{ app_root }} --env-file {{ docker_root }}/env/parameters.live.env down"
  become: yes
  tags:
    - services
    - services-stop