# Services Start

- name: "SERVER - Start application services"
  shell:
    cmd: "docker-compose --file {{ docker_root }}/docker-compose.yml --project-name dropship --project-directory {{ app_root }} --env-file {{ docker_root }}/env/parameters.live.env up -d"
  become: yes
  tags:
    - services
    - services-start